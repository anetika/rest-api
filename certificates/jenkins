pipeline {
    agent any
    options {
        buildDiscarder(logRotator(numToKeepStr: '5'))
        }
        stages {
            stage('Scan') {
                steps {
                    dir('./certificates') {
                        sh 'pwd'
                        sh ".\gradlew.bat clean build codeCoverageReport -Dsonar.host.url=http://localhost:9000 -Dsonar.login=admin -Dsonar.password=BY_ak-777777 sonarqube"
                    }
                    withSonarQubeEnv(installationName: 'sonarqube') {

                    }
                }
            }
        }
    }